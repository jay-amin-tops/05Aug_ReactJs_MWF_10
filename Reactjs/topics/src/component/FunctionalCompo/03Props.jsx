import React from 'react';

const PropsinFunctional = (props) => {
    return (
        <div>
            child compo
            {props.title}
        </div>
    );
};

export default PropsinFunctional;